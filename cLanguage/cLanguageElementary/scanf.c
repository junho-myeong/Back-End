#define _CRT_SECURE_NO_WARNINGS // 비쥬얼 스튜디오에서만 넣으면 된다. 입력값에 길이를 지정할수 없기때문에 보안에 취약해서 이것을 넣어줘야 scanf작동 단 이게 없으면 scanf_s를 사용한다
#include <stdio.h>

int main()
{
	// 정수 입력 받기
	/*
	int num1;
	printf("정수를 입력해주세요!\n");
	scanf("%d", &num1); // 첫번째 파라미터는 서식 지정자, 두번째 파라미터는 입력값을 저장할 변수를 넣는다
	printf("%d", num1);
	// scanf와 scanf_s에 차이점은 scanf는 오래된 함수이기 때문에 보안에 취약하다는 단점을 가지고 잇어서 사용하지 말라고 권고한다
	// 그래서 맨위에 줄이 없으면 컴파일 에러가 발생한다.

	int num2;
	int num3;
	printf("두개의 정수를 입력해주세요!\n");
	scanf("%d %d", &num2, &num3); // scanf 함수에는 변수 앞에 반드시 &를 넣어 줘야한다!!
	printf("%d %d", num2, num3);
	*/

	// 실수 입력 받기

	return 0;
}